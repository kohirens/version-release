description: >
  Trigger a workflow to publish a tag to make a new release in the version control system.
parameters:
  triggered_by_bot:
    description: Trigger publishing a release tag workflow.
    type: boolean
    default: false
usage:
  version: 2.1

  orbs:
    vro: kohirens/version-release@latest

  workflows:
    version: 2
    publish-release-tag: # Run ONLY on the main branch, and parameter triggered_by_bot is true, and when kicked-off by the webhook or api.
      when:
        and:
          - equal: [main, << pipeline.git.branch >>]
          - << pipeline.parameters.triggered_by_bot >>
          - matches:
              pattern: "^api|webhook$"
              value: << pipeline.trigger_source >>
      jobs:
        - vro/tag-and-release:
            context: orb-publishing
