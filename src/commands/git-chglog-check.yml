description: >
  Check for a git-chglog configuration file.
parameters:
  configFile:
    type: string
    default: ".chglog/config.yml"
    description: Location of a git-chglog configuration file.
steps:
  - run:
      name: Check for config
      command: << include(scripts/git-chglog-check.sh) >>
