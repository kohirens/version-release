description: >
  Update the CHANGELOG.md file.
parameters:
  outputFile:
    description: Location to output/update the CHANGELOG file.
    type: string
    default: "CHANGELOG.md"
  config:
    description: Location of a git-chglog configuration file.
    type: string
    default: ".chglog/config.yml"
steps:
  - run:
      name: Updating the CHANGLOG.md
      command: |
        wd=$(pwd)
        printf "working directory: ${wd}\n"
        git-chglog --output <<parameters.outputFile>> -c <<parameters.config>>
